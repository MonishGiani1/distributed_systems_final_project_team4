# Dockerfile.legitimate - Single container with 200 legitimate users

FROM python:3.9-slim

WORKDIR /app

COPY requirements_users.txt .
RUN pip install --no-cache-dir -r requirements_users.txt

COPY legitimate_pool.py .

CMD ["python", "legitimate_pool.py"]