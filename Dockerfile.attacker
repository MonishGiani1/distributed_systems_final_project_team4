# Dockerfile.attacker - Single container with 200 attacker users

FROM python:3.9-slim

WORKDIR /app

COPY requirements_users.txt .
RUN pip install --no-cache-dir -r requirements_users.txt

COPY attacker_pool.py .

CMD ["python", "attacker_pool.py"]